#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include "Parser/parser.h"
#include "Utilities/bitManipulation.h"

int main(int argc, const char **argv) {
	// if(parse(argc, argv)) {
	// 	return 1;
	// }
	return 0;
}
