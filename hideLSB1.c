#include <stdio.h>

// TODO: continue this
// void hideLSB1(const char* carrier, const int sampleRate, const int carrierLength, const char* data, char* output) {
//   output = malloc(carrierLength);
//   int i, k = 0;
//   int increment = sampleRate / sizeOf(char);
//   for (i = 0; i < carrierLength; i++) {
//     if (i % 2 == 1) {
//
//       // when its the last char of the sample
//       // change last bit first.
//       char copy = carrier[i];
//       copy[7] = data[k];
//       tocopy = carrier + i * sizeof(char);
//
//     } else {
//       // every other char
//       tocopy = carrier + i * sizeof(char);
//     }
//
//     memcpy(output + i * sizeOf(char), tocopy, sizeOf(char));
//
//
//   }
//
//   // output = malloc()
// }
